{
  "name": "schema-markup-generator-plugin",
  "description": "Generate production-ready JSON-LD schema.org markup for any page URL — always includes CometChat's approved Organization + WebSite, custom-builds the rest as a linked @graph, validates before returning, and outputs paste-ready raw JSON-LD for the CMS (Storyblok Structured Data Markup field). Validate-only; a human deploys.",
  "version": "1.1.0",
  "author": {
    "name": "Vishal Singh",
    "email": "vishal.singh@cometchat.com"
  },
  "keywords": ["seo", "schema.org", "structured data", "json-ld", "rich results", "marketing"]
}
